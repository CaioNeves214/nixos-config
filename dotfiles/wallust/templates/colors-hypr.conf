# Gerado por wallust — NÃO editar (incluído por hyprland.conf via `source`).
# Design tokens: as 5 cores do design system, no formato rgb() do Hyprland.
$base      = rgb({{background | strip}})
$text      = rgb({{foreground | strip}})
$primary   = rgb({{color4 | strip}})
$secondary = rgb({{color2 | strip}})
$alert     = rgb({{color1 | strip}})
