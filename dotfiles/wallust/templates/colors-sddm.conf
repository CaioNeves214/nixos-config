# Gerado por wallust — NÃO editar (lido pelo Main.qml do tema de login).
# Design tokens: as 5 cores do design system, em key=valor.
base={{background}}
text={{foreground}}
primary={{color4}}
secondary={{color2}}
alert={{color1}}
